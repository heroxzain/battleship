import Gameboard from "./Gameboard";
import Ship from "./ship";

describe.skip('', () => {
    beforeEach(() => {

    });

    test('', () => {
        expect().toBe();
    });
});


/*
Ships: 
* Cruiser: 5
* BattleShip: 4
* Destroyer: 3
* Submarine: 2
* PatrolBoat: 1

const ships = [
    {coords = [x, y], length = 5, axis = x or y},
    {coords = [x, y], length = 4, axis = x or y},
    {coords = [x, y], length = 3, axis = x or y},
    {coords = [x, y], length = 2, axis = x or y},
    {coords = [x, y], length = 2, axis = x or y},
    {coords = [x, y], length = 1, axis = x or y},
    {coords = [x, y], length = 1, axis = x or y},
];
*/
